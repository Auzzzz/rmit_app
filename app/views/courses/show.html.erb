
    <h1><%= @course.name %></h1>
  <p><h4>Category: </h4><% @course.categories.each do |category| %>
    <%= link_to category.name, courses_path(:id => category.id, :table => "Category") %></p>
  <% end %>
    <p><h4>Location: </h4><% @course.locations.each do |location| %>
    <%= link_to location.name, courses_path(:id => location.id, :table => "Location") %> </p>
  <% end %>
<p><h4>Course Description: </h4><%= @course.desc %> </p>


