<div class="form">
    <%= form_for(@location) do |f| %>
       
        <div class="form-group">
          <%= f.label :name %>
          <%= f.text_field :name, class: 'form-control', id: 'location', placeholder: 'e.g. 001.01.100' %>
        </div>
        <%= f.submit class: 'btn btn-primary btn-primary--wide' %>
    <% end %>
</div>